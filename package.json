{
  "name": "asistente-personal-ia",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["frontend", "worker"],
  "scripts": {
    "dev:frontend": "npm run dev --workspace frontend",
    "dev:worker": "npm run dev --workspace worker",
    "build": "npm run build --workspace frontend && npm run typecheck --workspace worker",
    "typecheck": "npm run typecheck --workspace frontend && npm run typecheck --workspace worker"
  },
  "engines": {
    "node": ">=20.19"
  }
}
